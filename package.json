{
  "name": "signal-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A synthetic customer-feedback dashboard for the Copilot for Product demo.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "test:saved-views": "node --test test/saved-views.test.js",
    "validate": "node scripts/validate-artifacts.mjs",
    "issues:preview": "node scripts/roadmap-to-issues.mjs --initiative init-saved-views-preview",
    "issues:preview:json": "node scripts/roadmap-to-issues.mjs --initiative init-saved-views-preview --format json",
    "demo:check": "npm run validate && npm test && npm run issues:preview:json",
    "demo:reset": "node scripts/reset-demo.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
